apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../config/default

images:
  - name: controller
    newName: noctaya.io/noctaya
    newTag: e2e

patches:
  - target:
      kind: Deployment
      labelSelector: control-plane=controller-manager
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/args
        value:
          - --metrics-bind-address=:8443
          - --health-probe-bind-address=:8081
          - --leader-elect
          - --gateway-image=noctaya.io/noctaya-gateway:e2e
          - --gateway-replicas=1
