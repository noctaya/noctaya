{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "noctaya.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "noctaya.labels" . | nindent 4 }}
{{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "noctaya.fullname" . }}-manager-role
  labels: {{- include "noctaya.labels" . | nindent 4 }}
rules:
- apiGroups: [""]
  resources: [persistentvolumeclaims, services]
  verbs: [create, delete, get, list, patch, update, watch]
- apiGroups: [apps]
  resources: [deployments]
  verbs: [create, delete, get, list, patch, update, watch]
- apiGroups: [batch]
  resources: [jobs]
  verbs: [create, delete, get, list, patch, update, watch]
# KEDA is installed independently; Noctaya manages only each LLMService's ScaledObject.
- apiGroups: [keda.sh]
  resources: [scaledobjects]
  verbs: [create, delete, get, list, patch, update, watch]
- apiGroups: [serving.noctaya.io]
  resources: [inferenceruntimes, llmservices]
  verbs: [get, list, watch]
- apiGroups: [serving.noctaya.io]
  resources: [llmservices/status]
  verbs: [get, patch, update]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "noctaya.fullname" . }}-manager-rolebinding
  labels: {{- include "noctaya.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "noctaya.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "noctaya.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- if .Values.operator.leaderElect }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "noctaya.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels: {{- include "noctaya.labels" . | nindent 4 }}
rules:
- apiGroups: [""]
  resources: [configmaps]
  verbs: [get, list, watch, create, update, patch, delete]
- apiGroups: [coordination.k8s.io]
  resources: [leases]
  verbs: [get, list, watch, create, update, patch, delete]
- apiGroups: [""]
  resources: [events]
  verbs: [create, patch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "noctaya.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels: {{- include "noctaya.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "noctaya.fullname" . }}-leader-election
subjects:
- kind: ServiceAccount
  name: {{ include "noctaya.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
