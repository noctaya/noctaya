image:
  registry: ghcr.io/noctaya
  operator: noctaya
  gateway: noctaya-gateway
  tag: ""
  pullPolicy: IfNotPresent

operator:
  replicas: 1
  leaderElect: false
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 10m
      memory: 128Mi

gateway:
  # One replica is required until demand aggregation is available.
  replicas: 1

serviceAccount:
  create: true
  name: ""

nameOverride: ""
